[build]
builder = "nixpacks"

[deploy]
startCommand = "php artisan migrate --force && chmod -R 755 public/build && php -S 0.0.0.0:${PORT} -t public"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
