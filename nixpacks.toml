[variables]
NODE_VERSION = "20"

[phases.install]
cmds = [
  "composer install --no-dev --prefer-dist --optimize-autoloader",
  "npm ci"
]

[phases.build]
cmds = [
  "php artisan key:generate --force || true",
  "npm run build"
]


